import React from "react";
import { Typography, Box } from "@mui/material";

function Navbar() {
  return (
    <Box sx={{ textAlign: "center", backgroundColor: "pink" }}>
      <Typography variant="h1">Tijerino</Typography>
      <Typography variant="h2">Cakes and Pastries</Typography>
    </Box>
  );
}
export default Navbar;
